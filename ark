#!/bin/bash
export PATH="$HOME/.bun/bin:$PATH"
ARK_ROOT="$(cd "$(dirname "$(readlink -f "$0" 2>/dev/null || realpath "$0" 2>/dev/null || echo "$0")")" && pwd)"
bun "$ARK_ROOT/packages/cli/index.ts" "$@"
