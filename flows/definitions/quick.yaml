name: quick
description: "Quick flow: implement → PR → build (no planning or review stages)"
stages:
  - name: implement
    agent: implementer
    gate: auto
    on_failure: "retry(3)"

  - name: pr
    action: create_pr
    gate: auto

  - name: build
    action: wait_flow
    gate: auto
    on_failure: notify

  - name: merge
    action: merge_pr
    gate: manual
