name: islc-execute-subtask
description: "One subtask in the islc execute fan-out -- runs the task-implementer agent on one subtask."
requires_repo: truestages:
  - name: implement
    agent: task-implementer
    gate: auto
    task: |
      {{summary}}
