// Stub for react-devtools-core — Ink imports this conditionally (DEV=true only).
// The binary build uses --external to skip bundling, but still needs a resolvable module.
export default { connectToDevTools() {} };
