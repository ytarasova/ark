name: islc
description: "Full ISLC pipeline from Jira ticket to merged PR"
flow: islc
variables:
  - name: ticket
    description: "Jira ticket key (e.g. IN-1234)"
    required: true
  - name: repo
    description: Repository path
    required: true
summary: "ISLC: {{ticket}}"
