name: self-quick
description: |
  Quick single-agent dispatch against the ark repo. For trivial tasks
  (docs, config, small fixes) that don't need plan/review stages.
  Uses the autonomous flow (single work stage, gate: auto).

repo: "."
flow: autonomous
agent: worker

variables:
  - name: summary
    description: What the agent should do
    required: true
